<h1>Decks</h1>
<br>
<p><%= link_to  "Meus decks", my_decks_path %></p>
<p><%= link_to  "Criar deck", new_deck_path %></p>
<%= flash[:alert] %>
<br>

<ul>
<% @decks.each do |deck| %>
<li><%= link_to deck.name, deck_path(deck.id) %></li>
<% end %>
</ul>

<%= link_to  "Voltar", root_path   %>
